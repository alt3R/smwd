<h1>Tracked People</h1>
<ul>
  <% @people.each do |person| %>
    <li>
      <%= link_to(person.full_name != 'undefined' ? person.full_name : person.username, person) %>
    </li>
  <% end %>
</ul>
<%= link_to "New Tracked Person", new_person_path %>
